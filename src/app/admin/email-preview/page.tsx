import CoachEmail from "@/components/CoachEmail";

/**
 * Preview the assignment email design. Backend should replicate this in the HTML email.
 * Add public/coach-video-thumb.jpg for the video thumbnail image.
 */
export default function EmailPreviewPage() {
  return <CoachEmail />;
}
