import LoadingState from "@/components/willab/LoadingState";

/** Route-level wait: the one circular logo loader, fullscreen. */
export default function Loading() {
  return <LoadingState fullscreen />;
}
